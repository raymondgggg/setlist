extend type Query {
  user(id: ID!): User
  users: [User!]!
}

extend type Mutation {
  createUser(input: NewUser!): User!
}

type User {
  id: ID!
  firstName: String
  lastName: String
  email: String!
}

input NewUser {
  firstName: String
  lastName: String
  email: String!
}
